#include <iostream>
using namespace std;
int main() {
    //Program to find the reverse of a given number.
    
    // int x, rev = 0;
    // cin >> x;

    // while(x>0) {
    //     int last = x%10;        
    //     rev = rev*10+last;
    //     x = x/10;

    // }
    // cout << rev;

    //Program to find if a given number is a palindrome or not.

    // int x, rev=0;
    // cin >> x;

    // while (x>0) {
    //     int last = x&10;
    //     rev = rev*10+last;
    //     x = x/10;
    // }
    
    // x==rev?cout << "YES":cout << "No";


    // int x;
    // cin >> x;

    // for (int i=1; i<=10; ++i) {
    //     cout << x << " X " << i << " = " << x*i << endl;
    // }

    // int x, count = 0;
    // cin >> x;

    // if (x==0) {
    //     cout << 1;
    // }
    // else {
    //     while (x>0) {
    //     x=x/10;
    //     ++count;

    // }
    // cout << count;
    // }

    //WAP to find the factorial of a given number;

    // int x, fact=1;
    // cin >> x;
    
    // for (int i=1; i<=x; ++i) {
    //     fact=fact*i;

    // }
    // cout << fact;

    //WAP to find the sum of digits of a number.

    // int x, last, sum=0;
    // cin >> x;

    // while (x>0) {
    //     last = x%10;
    //     sum = sum + last;
    //     x = x/10;
    // }
    // cout << sum;


}